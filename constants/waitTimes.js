// Wait time in seconds
const waitTimes = {
    waitTimeBetweenSelectors: 60,
    waitTimeBetweenActions: 3,
};

const waitTimeBetweenSelectors = waitTimes.timeBetweenSelectors;
const waitTimeBetweenActions = waitTimes.timeBetweenActions;

export { 
    waitTimeBetweenSelectors,
    waitTimeBetweenActions
};